 #!/bin/sh

SRC=$(cd $(dirname "$0"); pwd)/../olimcc
HEAD=`git rev-parse HEAD | cut -c -10`
REMOTE="~/apps/incoming"

cd $SRC
lein ring uberwar
FILE=`find $SRC/*.war | head -1`
FILENAME=$(basename $FILE)

echo "Pushing WAR to: $HEAD-$FILENAME"
scp $FILE ubert@ec2:$REMOTE/$HEAD-$FILENAME

rm $FILE

