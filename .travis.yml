sudo: required #is required to use docker service in travis

language: python

services:
  - docker

install:
  - echo "install nothing!" # put your normal pre-testing installs here

script:
  - echo "no tests!"

after_success:
  - cd olimcc
  - docker --version
  - pip install awscli
  - export PATH=$PATH:$HOME/.local/bin
  - aws s3 cp $APP_CONFIG_FILE ./.app-config.clj
  - eval $(aws ecr get-login --region us-east-1)
  - docker build -t olimcc-app .
  - docker tag olimcc-app:latest $AWS_ECS_REGISTRY/olimcc-app:latest
  - docker push $AWS_ECS_REGISTRY/olimcc-app:latest
